<Page
    x:Class="DiscordNukeBot.Pages.BotPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DiscordNukeBot.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <Button x:Name="StartBotButton" Content="Start Bot" Click="StartBotButton_Click"/>
            <Button x:Name="StopBotButton" Content="Stop Bot" Click="StopBotButton_Click" IsEnabled="False"/>
            <ProgressRing x:Name="BotStatusIndicator" IsActive="False" Margin="10,0,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <TextBox x:Name="LogOutput"
                     Header="Log Output"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     FontFamily="Consolas"/>
        </ScrollViewer>
    </Grid>
</Page>

